apiVersion: v1
kind: Service
metadata:
  name: subscription-management
  labels:
    app: subscription-management
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: subscription-management